#include <stdio.h>
int main() {
	// char c;
	// int i, j, k;
	// do {
	// 	printf("Enter the number: ");
	// 	scanf("%d", &n);
	// 	if(n < 0 && )
	// 	if()
	// }
	char n;
	scanf("%c", &n);
	printf("ASCII value of %c = %d\n", n, n);

	return 0;
}